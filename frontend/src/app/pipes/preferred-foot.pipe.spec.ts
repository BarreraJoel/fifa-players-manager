import { PreferredFootPipe } from './preferred-foot.pipe';

describe('PreferredFootPipe', () => {
  it('create an instance', () => {
    const pipe = new PreferredFootPipe();
    expect(pipe).toBeTruthy();
  });
});
